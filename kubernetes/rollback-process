If you already deployed v5.0 and want to rollback
"kubectl rollout undo deployment <deployment-name>"

If you want to rollback to a specific version (v4.0 image)
" deployment.yaml

spec:
  containers:
    - name: my-app
      image: myrepo/myapp:v4.0

kubectl apply -f deployment.yaml "

